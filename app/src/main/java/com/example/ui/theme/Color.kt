package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Sleek Interface Color Palette
val SlateDarkBg = Color(0xFFFDF8F6)       // Warm peach/ivory canvas background
val SurfaceGlass = Color(0xFFFFFFFF)      // Sleek pure white cards / dialogs
val SurfaceGlassElevated = Color(0xFFE7E0EC) // Light lavender/grey borders and elements

val GoldPremium = Color(0xFF6750A4)       // Primary brand purple accents
val GoldLight = Color(0xFFE8DEF8)         // Soft warm lavender container/accent
val BronzeWarm = Color(0xFFD1E1FF)        // Soft light blue container/accent

val TextPrimary = Color(0xFF1C1B1F)       // Deep charcoal primary text
val TextSecondary = Color(0xFF49454F)     // Soft medium gray-purple text
val TextMuted = Color(0xFF625B71)         // Subtle slate purple-gray

val StatusLiveGreen = Color(0xFF10B981)   // Operational status: Green
val StatusAlertAmber = Color(0xFFF59E0B)  // Maintenance status: Orange
val StatusErrorRed = Color(0xFFEF4444)    // Cancelled / High Alert: Red
val StatusPaidBlue = Color(0xFF6750A4)    // Financial / Paid state: Purple / Primary brand

